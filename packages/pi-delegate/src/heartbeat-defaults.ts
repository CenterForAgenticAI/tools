/** Default heartbeat interval (3 minutes) when `heartbeatIntervalMs` is absent. */
export const DEFAULT_HEARTBEAT_INTERVAL_MS = 180_000;

/** Default consecutive-heartbeat cap before auto-escalation. */
export const DEFAULT_MAX_CONSECUTIVE_HEARTBEATS = 5;
